{
  "name": "smartcare-api-server",
  "version": "1.0.0",
  "description": "Fake API server for SmartCare frontend development",
  "main": "db.json",
  "scripts": {
    "start": "json-server --watch db.json --port 3002 --host 0.0.0.0"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
